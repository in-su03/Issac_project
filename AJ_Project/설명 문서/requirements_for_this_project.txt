# ============================================================
# requirements_for_this_project.txt
# 현재 AJ_Project(두산 A0509 재구성 버전) 기준.
#
# 검증 환경: Python 3.8.20 / conda(issac_env) / Ubuntu / CUDA 12.1
# ============================================================

# --- 현재 run.py + doosan_a0509_controller 가 직접 사용 (필수) ---
numpy==1.24.4
ikpy==3.3.4        # 좌표↔관절각 역기구학(IK)

# --- 에셋 제작(STEP/STL → URDF 변환) 시에만 사용, 런타임엔 불필요 ---
# trimesh==4.12.2
# cascadio            # STEP 리더 (OpenCASCADE 기반)
# fast-simplification # 메쉬 데시메이션

# ============================================================
# isaacgym 은 PyPI에 없는 NVIDIA 전용 패키지 → 이 파일로 설치 안 됨.
#   별도로:  pip install -e ~/isaacgym/python
# ============================================================

# ============================================================
# isaacgym 은 PyPI에 없는 NVIDIA 전용 패키지 → 이 파일로 설치 안 됨.
#   별도로:  pip install -e ~/isaacgym/python
#   (활성 코드 22파일이 import. torch/numpy/scipy 등 핵심 의존성도
#    isaacgym setup.py가 함께 요구하므로 위 목록과 중복돼도 무방)
# ============================================================
